---

- hosts: centos
  
  tasks:

    - name: Copy file
      template:
        src: ./test.enc
        dest: /tmp/test.txt
        mode: 0600
        owner: ben
      become: yes
      become_user: root
