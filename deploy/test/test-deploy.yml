---

- hosts: centos
 
  tasks:

    - name: Copy file
      template:
        src: ./test.enc
        dest: /tmp/test.txt
        mode: 0600
        owner: ben
      become: true
      become_user: root
